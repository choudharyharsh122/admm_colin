This folder is a cantilever-beam variant of the `oc_method` implementation.

Files are copied from the original `oc_method` folder. Update any problem-
specific parameters (mesh, loads, boundary conditions) to represent the
cantilever beam before running scripts.
