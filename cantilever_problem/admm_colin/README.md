This folder is a cantilever-beam variant of the `admm_colin` implementation.

Files are copied from the original `admm_colin` folder to preserve the same
workflow; adjust configuration files (e.g. `admm_config.cfg`) to set cantilever
problem parameters before running.
